---
description: Search the user's MeloTune library and recommendations for a track, artist, or vibe.
---

Call the `melotune_search` MCP tool with the user's query text as the `query` argument. The query is everything the user typed after the command. Present the returned text directly to the user.
