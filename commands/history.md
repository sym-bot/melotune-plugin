---
description: Show recent listening history from MeloTune — recent tracks, top artists, top moods.
---

Call the `melotune_listening_history` MCP tool. If the user specified a number after the command, pass it as the `limit` argument. Present the returned text directly to the user.
